# v0.12 Synthetic Cross-Module Integration Scenario

Status: synthetic-only assurance scenario. It exercises repository-owned empty
fixtures and test-owned temporary directories. It is not a real study, data
provenance record, manuscript, installation receipt, or release authorization.

## Sequence

1. Request the existing empty-workspace bootstrap preview and confirm the exact
   generated plan with a synthetic approval reference.
2. Structurally validate the existing metadata-only provenance register set.
3. Structurally validate the existing workflow/evidence-control bundle.
4. Structurally validate the existing lesson-promotion-control bundle.
5. Check that the five manuscript-governance templates remain blank and require
   accountable-human review.
6. Simulate a source-copy update and rollback under one test-owned temporary
   root, preserving the prior copy and verifying content identity.

## Boundaries

The scenario does not read, copy, hash, clean, analyze, share, or otherwise
access real data. It does not read a source target, URL, credential, project,
manuscript, reviewer correspondence, declaration, or submission package.

The temporary copy in step 6 is not a private skill source or installed Codex
runtime. A passing result does not prove that a real installation, update, or
rollback occurred. It also does not prove data access, scientific truth,
evidence support, compliance, ethics, human authority, publication readiness,
or submission permission.

If a named input is invalid, unknown, outside the test root, linked, or outside
the documented synthetic set, stop the affected operation and request the
appropriate human review.
