name: Test controlled bootstrap

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.14"]
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - name: Check out the exact released framework commit for integration validation
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          repository: chenhaoran2068/governed-research-workspace-framework
          ref: b0e32d7710b70299e633df1316b6924cd87b647b
          path: _framework_release
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install framework validation dependencies
        run: python -m pip install --disable-pip-version-check -r _framework_release/requirements-dev.txt
      - name: Run standalone and cross-repository integration tests
        env:
          FRAMEWORK_REPOSITORY_ROOT: ${{ github.workspace }}/_framework_release
          FRAMEWORK_RELEASE_TAG: v0.1.1
          FRAMEWORK_EXPECTED_COMMIT: b0e32d7710b70299e633df1316b6924cd87b647b
        run: python -m unittest discover -s tests -v
