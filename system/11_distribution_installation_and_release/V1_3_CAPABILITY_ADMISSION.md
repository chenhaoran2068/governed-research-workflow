# V1.3.0 Capability-Admission Review

Status: commit-neutral pre-C4 capability-admission snapshot. The accountable
human admitted `GRW-CAP-140-01` for the v1.3.0 versioned source scope. This
record is not C4, an exact commit, an annotated tag, a GitHub Release, or an
installed-runtime statement.

## Versioned Source Scope

`GRW-CAP-140-01`, Proportionate Historical-Experience Review And
Retrieval-Index Contract, provides only generic metadata-only schemas, blank
templates, caller-named read-only validators, synthetic fixtures, tests, and
guidance for an L1 final-disposition register and an optional retrieval-index
link to a named mapped decision.

The four outcomes are `mapped`, `not_mapped`, `deferred`, and `blocked`. A
named batch may contain at most twenty decisions, but each source still needs
its own accountable-human final decision. The record has no authority beyond
representing that decision and cannot establish source truth, currentness,
identity, authority, access, maturity, reuse, promotion, or rule integration.

## Non-Goals

This scope does not publish a real vocabulary, inventory, source, locator,
path, hash, mapping, or decision. It does not read source bodies or pointers,
discover files, infer terms, create a decision, perform mapping, promote or
integrate experience, provide intake/upload/RAG/network action, change the
Framework, create an agent runtime, install a runtime, or make a Release
decision.

## Remaining Boundary

Before public availability can be asserted, separately controlled exact
candidate, remote CI, protected-main merge, exact C4 decision, annotated tag,
matching GitHub Release, and post-release verification remain required. A
separate controlled local-adoption task is required before any runtime claim.
