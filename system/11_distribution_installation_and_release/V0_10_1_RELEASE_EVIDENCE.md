# v0.10.1 Release Evidence: Pre-C3-Remote Preparation Record

Status: pre-C3-remote preparation evidence. It preserves local candidate and
private synthetic-pilot evidence, and identifies what must be repeated for an
exact remote candidate. It is not a C3 authorization, hosted CI result, C4
authorization, Git tag, GitHub Release, normal installation target, or runtime
identity.

## Candidate Basis

| Field | Recorded value |
| --- | --- |
| Public baseline | immutable `v0.10.0`, commit `3caab6f5231ce0e7f97ad34efe5ea1d302e61289` |
| Initial local implementation commit | `dee2861` (`feat: add synthetic experience exchange pilot protocol`) |
| Intended version | `v0.10.1` |
| Intended capability set | `GRW-CAP-101-01` only |
| Framework integration identity | `v0.1.2`, commit `97fbd1f4f3cbaabb2cdbb3e106c91a6c9fd8b3a8` |
| Exact C3-remote candidate | deliberately unresolved until final preparation is committed and independently reviewed |
| Exact C4 release identity | deliberately unresolved until protected-main merge and a separate accountable-human C4 decision |

## Local And Pilot Evidence Boundary

- The complete local suite, focused positive/negative exchange-pilot tests,
  public-material scan, schema validation, no-write checks, and cross-line-
  ending hash regression must be rerun against the later exact candidate.
- The private maintainer-only synthetic pilot preserved later correction and
  future-use-stop commits rather than rewriting history. Its clean-clone check
  is useful controlled transport evidence only: it is not an external
  contributor, physical Computer B, rights, consent, safety, or real-transfer
  trial.
- The new receipt hash normalizes only physical CRLF/CR line endings for
  validated UTF-8 JSON metadata. It does not inspect, copy, upload, or hash
  real data or source content.

## Required Refresh Before C4

1. create and review an exact tracked-source candidate commit;
2. rerun the full local regression suite, focused v0.10/v0.10.1 tests, and
   static public-boundary review for that commit;
3. run the exact candidate on Windows, Ubuntu, and macOS CI;
4. review the protected-main merge commit and recheck capability scope,
   material/rights/privacy/secret, dependency/license, workflow, branch-
   protection, and immutable-release evidence;
5. obtain an accountable-human C4 decision naming the exact main commit,
   `v0.10.1` tag, release notes, and hosted Release operation; and
6. after publication, independently verify the immutable tag, matching GitHub
   Release, source archives, and no automatic private-source/runtime change.
