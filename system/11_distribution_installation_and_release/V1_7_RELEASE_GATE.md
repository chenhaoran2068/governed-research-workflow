# v1.7.0 Release Gate

## Candidate Scope

The candidate scope is only `GRW-CAP-170-01`: two generic public
collaboration-guidance documents, three generic public cards, three public
topics, and compatible public-card validation. It retains `GRW-CAP-160-01`
and all frozen v1 interfaces.

## Required Gate Evidence

1. The candidate starts from exact public `v1.6.0`.
2. No public file contains private source, decision, mapping, path, receipt,
   identifier, Study, project, or unpublished-material content.
3. The original 38 cards retain their KGE references; only the three new cards
   use null legacy identifiers.
4. Required local tests and two affected-scope review rounds pass.
5. A later remote matrix reports Windows, Ubuntu, and macOS success.
6. Protected-main merge and C4 are separately evidenced before a selected
   version can be treated as published.

## Non-Claim

This candidate gate does not prove a hosted Release, private canonical source,
or installed runtime.
