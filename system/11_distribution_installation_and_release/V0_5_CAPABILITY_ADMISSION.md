# v0.5.0 Capability Admission Record

Status: historical pre-C4 admission record. On 2026-07-18, the accountable
human admitted `GRW-CAP-050-01` for the named `v0.5.0` scope and confirmed
authority to publish the generic source tree under Apache-2.0. The later
published v0.5.0 tag and matching GitHub Release are verified separately by
the current release-status rule. This record preserves the admission decision;
it does not prove a local installation, runtime identity, legal certification,
or a future C4 authorization.

## Admission Basis And Effect

`GRW-CAP-050-01` is verified and has current interface/evidence references in
the capability ledger. The accountable-human admission permitted pre-C4
documentation to identify the bounded metadata-only capability as admitted. It
did not permit the AI to publish and did not replace the exact-final-commit
material, dependency, workflow, documentation, CI, C4, or post-release review
that later occurred.

## Admitted Disposition

| Capability | Historical admission state | Current hosted-release status |
| --- | --- | --- |
| `GRW-CAP-050-01` | verified and admitted for the named v0.5.0 scope | published at immutable `v0.5.0`; verify the exact tag and matching GitHub Release at installation time |

## Contradiction Stop

If the ledger, README, roadmap, schema, validator, tests, release-source
evidence, or version identity conflict, stop the affected claim or
consequential action. Present the evidence, unresolved items, and bounded
alternatives to the accountable human for decision.
