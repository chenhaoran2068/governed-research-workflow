# V1.2.0 Release Gate

Status: commit-neutral pre-C4 release-preparation gate snapshot. It is not an
exact commit, current remote-CI result, protected-main merge, C4 authorization,
annotated tag, GitHub Release, or installation statement.

## Intended Scope

The v1.2.0 versioned source scope contains one additive capability:
`GRW-CAP-120-01`, Controlled Experience Vocabulary And Reference-Index
Contracts. It provides generic metadata-only schemas, templates, caller-named
read-only structural validators, synthetic fixtures, tests, and guidance. The
frozen v1 public contract remains preserved separately.

No real-source reading, source-body/pointer action, source hashing, mapping,
promotion, rule integration, data action, generic writer, agent runtime,
external contribution intake, Framework change, Release action, or runtime
update is part of this scope.

## Gate Conditions

1. Candidate changes remain within the approved C3 and release-preparation
   file controls.
2. The four generic schemas, four blank templates, validators, synthetic
   fixtures, v1.2 ledger entry, release-preparation records, and frozen v1
   artifacts validate and cross-reference.
3. The exact later candidate commit passes refreshed local regression,
   material/secret review, dependency/workflow review, wording review, and
   candidate-identity review.
4. The accountable human has confirmed publication rights for the reviewed
   generic tree and admitted `GRW-CAP-120-01` for the v1.2.0 versioned source
   scope. Later exact content drift requires renewed review.
5. A separately authorized remote candidate passes Windows, Ubuntu, and macOS
   CI at Python 3.11 and 3.14 against the declared Framework binding.
6. A protected-main merge yields a new exact commit. That exact commit, final
   notes, tag, GitHub Release, immutable-release posture, and source archive
   require a separate C4 decision and post-release verification.

## Local Progress Boundary

This source snapshot records required release-gate conditions only. Exact local
regression evidence, candidate identity, remote CI, protected-main state, C4,
tag, Release, and runtime identity remain separately controlled. It must never
be used as a current public-availability or local-installation statement.
