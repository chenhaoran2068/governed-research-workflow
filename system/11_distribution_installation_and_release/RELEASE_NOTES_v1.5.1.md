# v1.5.1 Release Notes

Status: versioned pre-C4 source notes. These notes do not establish an exact
candidate commit, protected-main merge, annotated tag, GitHub Release, source
archive, or installed runtime.

## Compatibility Maintenance

- Retains `GRW-CAP-150-01` unchanged.
- Retains the two generic, optional, human-review-only manuscript-operation
  and research-program-boundary guidance documents unchanged.
- Declares framework-integrated compatibility only for the exact released
  Workspace Framework `v0.2.0` tag at commit
  `69c76f84a5b0913b26c17ea48f152dbc50b4bec6`.
- Adds synthetic regression coverage that an empty framework-integrated
  workspace can register this System without a generated `Papers/` root.

## Not Added

This maintenance source adds no capability, interface, schema, validator,
helper, dependency, material access, sharing permission, external action,
publication action, release decision, or runtime-installation behavior.

Before a selected v1.5.1 version may be treated as public or installed, verify
its exact annotated tag and matching GitHub Release, then complete the separate
controlled installation route.
