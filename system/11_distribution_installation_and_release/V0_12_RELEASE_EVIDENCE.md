# v0.12 Release Evidence: Local Pre-C3-Remote Preparation

Status: local release-preparation evidence for a no-new-interface synthetic
integration-assurance source. It is not an exact C3-remote candidate, hosted
CI result, protected-main identity, C4 authorization, tag, GitHub Release, or
runtime-installation statement. Any later change requires affected evidence to
be repeated under M48.

| Field | Recorded value |
| --- | --- |
| Intended version | `v0.12.0` |
| Public baseline | immutable `v0.11.0` at `317e6bf8f45ce728f9c1258a517a658ed6a8f7c5` |
| Exact local candidate | deliberately unresolved until release-preparation documents and tests are committed |
| Scope | no new capability or interface; synthetic assurance of existing bounded controls only |
| Local regression | 237 tests passed with no skips or failures using `PYTHONDONTWRITEBYTECODE=1` and the exact local Framework `v0.1.2` checkout at `97fbd1f4f3cbaabb2cdbb3e106c91a6c9fd8b3a8`; rerun after any later change |
| Candidate integrity | working candidate passed default `git diff --check` and `git fsck --no-dangling`; clean staged-tree review remains required before C3-remote |
| Static public-boundary review | selected local-path, project-marker, and credential scan passed; exact-candidate human public-rights confirmation remains required |
| Framework evidence | existing exact Framework `v0.1.2` identity retained; fresh three-platform CI remains required |
| C4 identity | deliberately absent from this local pre-C3-remote record |

The next exact candidate must bind refreshed local regression and static review
to a clean commit, receive separately authorized Windows, Ubuntu, and macOS
CI, and be reviewed after protected-main merge. Neither this record nor an
integration test result creates a hosted Release, installation target, C4
authorization, or runtime identity.
