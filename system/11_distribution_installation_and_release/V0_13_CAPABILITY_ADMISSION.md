# v0.13.0 Capability Admission: Local Pre-C3-Remote Record

Status: accountable-human C2 scope-admission record for the local v0.13
release-preparation tree. It is not C3-remote evidence, a pull request,
protected-main approval, C4 authorization, an annotated tag, a GitHub Release,
or an installed-runtime statement.

If a later C3-remote candidate or C4 Release is approved, this record remains
a historical local pre-C3-remote snapshot. It does not describe that later
exact candidate, hosted Release, or installed runtime.

## Exact Admitted Scope

The accountable human admitted only `GRW-CAP-130-01`, the Versioned V1 Support
Scope Matrix. It adds these bounded public interfaces:

- `system/00_manifest_and_profiles/v1_support_scope_matrix.json`, the sole
  machine-readable authority for module-level v1 support posture;
- `system/09_schemas_records_and_templates/v1_support_scope_matrix.schema.json`,
  its structural JSON Schema; and
- `system/00_manifest_and_profiles/V1_SUPPORT_SCOPE.md`, the human-readable
  guide to authority separation, compatibility, migration, maintenance, and
  refusal semantics.

The matrix covers every public module `00` through `12` and records a bounded
surface, external boundaries, v1 exclusions, compatibility/migration posture,
maintenance owner, evidence references, limitations, and a refusal statement.
It is additive. It does not replace the capability ledger's authority for an
individual capability claim.

## Required Boundaries

- All records and tests are generic, structural, and public-package only.
- The matrix does not add data access, project or manuscript authority,
  retrieval, source corpus, RAG, network activity, credentials, intake,
  upload/download, human-mediated curation, an agent runtime, coordinator,
  delegation, multi-agent orchestration, generic writer, installer, migration
  executor, recovery executor, automatic push, tag, Release, or runtime action.
- `active_bounded` means only that explicitly named, tested bounded surfaces
  exist. It never converts an external boundary or v1 exclusion into an
  available function.
- A structurally valid matrix does not prove that a capability is suitable for
  a task, that an external requirement is current, that a human decision is
  true, or that a public Release or local installation exists.

## Authority And Remaining Gates

The capability ledger records C2 source-scope admission for `GRW-CAP-130-01`.
That admission does not prove exact candidate identity, hosted availability,
runtime identity, or C4 authorization. The exact C3-remote candidate remains
unresolved while these preparation records are completed.

Before an annotated tag or GitHub Release can exist, a later exact candidate
needs refreshed local evidence, separately authorized Windows, Ubuntu, and
macOS CI, protected-main review, and a separate C4 decision. Public release,
private canonical-source adoption, and Codex runtime installation remain
separate controlled decisions.
