# V1.2.0 Capability-Admission Review

Status: commit-neutral pre-C4 capability-admission snapshot. The accountable
human admitted `GRW-CAP-120-01` for the v1.2.0 versioned source scope after
local implementation, candidate-content review, and release preparation. This
record is not C4, an exact commit, an annotated tag, a GitHub Release, or an
installed-runtime statement.

## Versioned Source Scope

`GRW-CAP-120-01`, Controlled Experience Vocabulary And Reference-Index
Contracts, provides only generic schemas, blank templates, caller-named
read-only structural validators, synthetic fixtures, tests, and guidance for:

- controlled experience terms;
- bounded source identities;
- represented accountable-human mapping decisions; and
- reference-index links.

It does not publish a real vocabulary or source inventory; read source bodies
or pointers; discover, hash, classify, tag, map, promote, or integrate an
experience; verify a person or authority; grant source access; provide intake,
upload, retrieval, RAG, external action, runtime installation, or a Release
decision.

## Evidence Reviewed Locally

- `tests/test_controlled_experience_vocabulary.py` covers generic vocabulary
  schema and structural-validator behavior.
- `tests/test_experience_reference_index.py` covers active-empty and synthetic
  reference-index structure, including negative mapping cases.
- `tests/test_v1_2_controlled_experience_vocabulary_contract.py` covers the
  public generic boundary, admitted ledger record, and no-release/no-install
  assertions.
- `tests/test_v1_2_release_preparation.py` covers the versioned release
  preparation chain, time-neutral current-facing wording, and pre-C4 identity
  boundary.
- Exact candidate identity, full-suite result, content snapshot, material
  review, and cache observation are retained in separately controlled C3
  evidence. This source record states no current count or transport outcome.

## Accountable-Human Admission

The accountable human accepted the v1.2 public derivative Charter and C2
scope, confirmed that the reviewed generic tree may be published by the
maintainer's GitHub account under Apache-2.0, and admitted `GRW-CAP-120-01`
for the v1.2.0 versioned source scope. Capability admission is distinct from
selected-version public availability and installed-runtime identity.

## Remaining Boundary

Before public availability can be asserted, a separately controlled exact
candidate, remote CI, protected-main merge, exact C4 decision, annotated tag,
matching GitHub Release, and post-release verification remain required. A
separate controlled local-adoption task is required before any runtime claim.
