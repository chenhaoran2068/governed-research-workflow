# Release Notes Source: v0.9.0 Integrity-Audit Controls

Status: pre-C4 release-notes source. It describes the admitted v0.9.0 scope
and does not itself establish publication, installation eligibility, C4,
runtime identity, or a hosted Release. Verify a selected version through its
exact annotated tag and matching GitHub Release.

## Release-Source Changes

- adds `integrity_audit_bundle.schema.json`, a blank synthetic template, and an
  explicitly invoked read-only structural validator for one finite metadata
  bundle;
- adds structural distinctions among audit observations, findings, harness
  metadata, correction/reassessment linkage, and operational-integrity
  preflight records;
- adds negative tests for unsafe paths and indirection, duplicate identities,
  unreliable harness evidence, unsafe recovery dispositions, and missing
  correction/re-review linkage; and
- records the v0.9 public-material, dependency/workflow, capability-admission,
  release-gate, release-control, and evidence boundaries.

## Explicit Non-Changes

v0.9.0 does not read or process real data, project material, receipts, runtime
state, hosts, URLs, credentials, Git repositories, or worktrees. It does not
verify a hosted Release, execute recovery, write a correction, choose a human
disposition, establish truth or authorization, add an agent runtime or
delegated role, make a scientific/compliance/Gate/submission decision, or
update a local runtime.

## Installation And Verification

Install only a selected version whose exact annotated tag and matching GitHub
Release resolve to the same reviewed commit. Do not install `main`, an
untagged branch, or an unreceipted local copy. A public Release does not
establish a private canonical source or installed Codex runtime identity.
