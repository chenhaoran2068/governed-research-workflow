# V1.4.0 Capability-Admission Review

Status: commit-neutral pre-C4 capability-admission snapshot. The accountable
human admitted GRW-CAP-140-02 for the v1.4.0 versioned source scope. This
record is not C4, an exact commit, an annotated tag, a GitHub Release, or an
installed-runtime statement.

## Versioned Source Scope

GRW-CAP-140-02, Controlled Experience Descriptor Profiles And Faceted
Retrieval Metadata, provides only generic metadata-only schemas, blank
templates, a caller-named read-only validator, synthetic fixtures, tests, and
guidance for a controlled descriptor catalogue, represented descriptor
decisions, and a derived descriptor index.

The five facets are domain, record kind, task trigger, target object, and
scope. An optional exact topic-term list remains separate from the facets and
from the v1.3 final-disposition register. A valid descriptor structure does not
establish source truth, currentness, evidence, approval, maturity, reuse,
promotion, ownership, access, human identity, or authority.

## Non-Goals

This scope does not publish a real descriptor catalogue, vocabulary term,
source identifier, inventory, source, locator, path, hash, decision, mapping,
or index entry. It does not read source bodies or pointers, discover files,
infer a descriptor, create a human decision, promote or integrate experience,
provide intake/upload/RAG/network action, change the Framework, create an
agent runtime, install a runtime, or make a Release decision.

## Remaining Boundary

Before public availability can be asserted, separately controlled exact
candidate, remote CI, protected-main merge, exact C4 decision, annotated tag,
matching GitHub Release, and post-release verification remain required. A
separate controlled local-adoption task is required before any runtime claim.
